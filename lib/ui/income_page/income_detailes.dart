import 'package:flutter/material.dart';

class DetailesIncome extends StatelessWidget {
  const DetailesIncome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
